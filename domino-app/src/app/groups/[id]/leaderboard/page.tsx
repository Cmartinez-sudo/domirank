export const dynamic = "force-dynamic";

export default async function GroupLeaderboardPage() {
  return (
    <div style={{ padding: "2rem", background: "#0a1020", color: "#10b981", fontFamily: "monospace" }}>
      MARKER_V3_MINIMAL_TEST — si ves esto, el deploy funcionó
    </div>
  );
}

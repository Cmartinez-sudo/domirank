import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function Dashboard() {
  return <PlaceholderScreen title="Inicio" subtitle="Tu dashboard vive aquí — próximamente." />;
}

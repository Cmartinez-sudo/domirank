import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function Leaderboard() {
  return <PlaceholderScreen title="Ranking" subtitle="Rankings globales y por grupo — próximamente." />;
}
